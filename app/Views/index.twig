<!DOCTYPE html>
<html lang="ja">
    <meta charset="UTF-8">
    <body>
        <h1>Hello, {{ last }} {{ first }}</h1>
        <div>
            <form action="/upload_file" method="post" enctype="multipart/form-data" >
                <div>
                    <input type="file" name="test_file">
                </div>
                <div>
                    <input type="submit" value="送信する">
                </div>
            </form>
        </div>
        <div>
            <img src={{ file_path }}>
        </div>
    </body>
</html>
